<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ProjetoFinalMauiReal.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:ProjetoFinalMauiReal.Pages"
    Shell.FlyoutBehavior="Disabled"
    Title="ProjetoFinalMauiReal">
    <ShellContent Title="Login" ContentTemplate="{DataTemplate pages:LoginPage}" Route="login" />
    <ShellContent Title="Consulta" ContentTemplate="{DataTemplate pages:ConsultaPage}" Route="consulta" />
    <ShellContent Title="Histórico" ContentTemplate="{DataTemplate pages:HistoricoPage}" Route="historico" />
</Shell>
